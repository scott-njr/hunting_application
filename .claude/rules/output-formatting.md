## Output Formatting

- Use markdown only when it will actually be rendered (chat, docs). Skip it in code comments and logs.
- Prefer code comments over explaining code in prose.
- Keep output scannable — use headers and bullets for multipart answers.
- When writing code, always include error handling.
- Log errors to the database, not just console.
